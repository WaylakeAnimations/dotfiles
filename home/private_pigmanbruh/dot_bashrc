#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '

alias schezmoi="sudo chezmoi --destination / --source /etc/chezmoi/dotfiles --working-tree /etc/chezmoi/dotfiles"
